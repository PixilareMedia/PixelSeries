---
export interface Props {
  title: string;
  img: string;
  series: string;
  anchor: string;
  id: string;
}

const { id, anchor, title, img, series } = Astro.props;
---

<div id={id} class="col justify-content-center text-center season-info">
  <h2 class="tertiary-title"><img class="header-logo" src=`/images/${series}/${img}.png` alt=`${title}`></h2>
  <a class="anchor-link" href={anchor}>(Link)</a>
  <div class="col justify-content-center">
    <slot/>
  </div>
</div>

<style>
  .header-logo {
    width: 15%;
    height: auto;
  }
  .season-info {
    border: 3px;
    border-color: lightslategray;
    border-style: inset;
    border-radius: 25px;
    font-size: large;
    padding: 10px;
    margin: 5px;
    background: rgba(51, 51, 51, 0.5);
    margin-bottom: 3em;
  }

  .anchor-link {
    color: white;
    text-decoration: none;
  }
  .anchor-link:hover {
    color: rgb(70, 33, 151);
  }
</style>